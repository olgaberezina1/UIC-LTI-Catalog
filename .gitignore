__pycache__/
.DS_Store

# SDD scratch: the .md artifacts (ledger, briefs, reports) are tracked
# deliberately so work can move between machines; the generated review
# diffs are not — regenerate them with scripts/review-package.
.superpowers/
